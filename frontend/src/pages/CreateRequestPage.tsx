import { ArrowLeft, Send } from "lucide-react";
import { Link } from "react-router-dom";
export function CreateRequestPage() { return <div className="field-page form-page"><Link className="back-link" to="/portal/requests"><ArrowLeft size={17} />Back to requests</Link><div className="field-hero"><p className="eyebrow">CUSTOMER PORTAL</p><h1>Create Request</h1><p>Submit a request for one of your sites.</p></div><form className="form-card" onSubmit={(e) => e.preventDefault()}><label>Site<select defaultValue="" disabled><option value="">Select your site</option></select><small>Your sites load from the API.</small></label><label>Title<input placeholder="Enter request title" required /></label><label>Description<textarea rows={5} placeholder="Describe the problem" required /></label><label>Priority<select defaultValue=""><option value="">Select priority if permitted</option></select></label><button className="primary-button full"><Send size={17} />Submit Request</button></form></div>; }
